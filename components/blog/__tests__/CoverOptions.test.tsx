// Simple test to check if component can be imported without crashing
import CoverOptions from "../CoverOptions";

describe("CoverOptions", () => {
  it("should be importable", () => {
    expect(CoverOptions).toBeDefined();
  });
});
